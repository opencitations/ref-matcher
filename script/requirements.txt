# Requirements for Reference Matching Tool
# Python 3.8+ required

# ============================================================================
# CORE DEPENDENCIES (Required)
# ============================================================================

# Fuzzy string matching for title comparison.
# rapidfuzz is the maintained, C++-backed successor to fuzzywuzzy: same
# fuzz.* API, much faster, and no separate python-Levenshtein dependency.
rapidfuzz>=3.0.0

# Unicode normalization
Unidecode>=1.3.6

# Progress bars
tqdm>=4.65.0

# Async HTTP client for OpenCitations queries
aiohttp>=3.9.0

# HTTP client used by the bundled GROBID client (data/grobid/grobid_client)
requests>=2.25.0

# Loading configuration from an external .env file
python-dotenv>=1.0.0
