# Requirements for Reference Matching Tool
# Python 3.8+ required

# ============================================================================
# CORE DEPENDENCIES (Required)
# ============================================================================

# Fuzzy string matching for title comparison
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.2  # Optional but highly recommended for speed

# Unicode normalization
Unidecode>=1.3.6

# Progress bars
tqdm>=4.65.0

# Async HTTP client for OpenCitations queries
aiohttp>=3.9.0
